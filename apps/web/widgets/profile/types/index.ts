export interface UserProfileType {
  user_id: string;
  username: string;
  email: string;
  address: string;
  address_detail: string;
  // credit, 온도 추가
}

export interface NavigationItem {
  label: string;
  href: string;
}
